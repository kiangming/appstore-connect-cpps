/**
 * Barrel export for cross-module reusable UI primitives (not tied to a
 * single tool's module, unlike `components/ui/iap/`).
 */
export { ExpandableErrorCell } from "./ExpandableErrorCell";
export type { ExpandableErrorCellProps } from "./ExpandableErrorCell";
